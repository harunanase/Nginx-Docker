version: "3"
services:
  web:
    image: "haru7se/nginx"
    ports:
      - "80:80"
    volumes:
      - ./conf/nginx.conf:/opt/nginx/conf/nginx.conf
      - ./conf/waf:/opt/nginx/conf/waf
      - ./conf/vhost:/opt/nginx/conf/vhost
      - ./logs:/opt/nginx/logs

